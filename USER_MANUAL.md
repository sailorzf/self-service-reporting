# 自助报表系统使用手册

## 目录

1. [系统概览](#1-系统概览)
2. [数据表管理](#2-数据表管理)
3. [数据导入](#3-数据导入)
4. [报表定制](#4-报表定制)
5. [报表分享与导出](#5-报表分享与导出)
6. [AI 辅助分析](#6-ai-辅助分析)

---

## 1. 系统概览

自助报表系统是一个基于 Web 的可视化报表平台，核心流程为：

```
注册数据表 → 导入Excel数据 → 设计报表 → 分享/导出
```

系统提供五个功能模块，通过左侧导航栏切换：

| 模块 | 路径 | 功能 |
|------|------|------|
| 数据表 | `/tables` | 注册和管理数据库表映射 |
| 数据导入 | `/import` | 上传 Excel 并导入到已注册的数据表 |
| 报表列表 | `/reports` | 查看、执行、编辑、分享、导出报表 |
| 报表设计 | `/reports/new` | 拖拽式设计报表画布 |
| 报表分享 | `/share/:token` | 通过链接查看已分享的报表 |

---

## 2. 数据表管理

数据表是系统的基础，它将 MySQL 中的物理表映射为系统可识别的逻辑表。所有后续的数据导入和报表查询都基于已注册的数据表。

### 2.1 进入数据表管理

点击导航栏中的 **数据表**，进入数据表管理页面。页面以表格形式展示已注册的数据表，包含标识、名称、数据库、物理表名、字段列表和创建时间。

### 2.2 新建数据表

点击 **新建数据表** 按钮，弹出创建对话框。支持两种创建方式：

#### 方式一：手动创建

切换到 **手动创建** 标签页，填写以下信息：

| 字段 | 说明 | 示例 |
|------|------|------|
| **标识** | 系统内部唯一标识，用于 API 和报表引用 | `operation` |
| **名称** | 界面展示名称 | `运营数据` |
| **物理表名** | MySQL 中的实际表名，不能与已有表重名 | `data_operation` |
| **数据库名** | 可选，表所在的数据库。留空表示使用默认数据库 | `analytics_db` |

**字段定义：**

1. 点击 **+ 添加字段** 添加列
2. 为每个字段配置以下属性：

| 属性 | 说明 |
|------|------|
| **字段名** | 列的名称 |
| **类型** | varchar / decimal / int / datetime / text |
| **长度** | varchar 类型的字符长度（默认 255） |
| **精度** | decimal 类型的总位数（默认 10） |
| **小数位** | decimal 类型的小数位数（默认 2） |
| **可空** | 是否允许为空值 |

3. 完成后点击 **创建**，系统会在 MySQL 中自动创建对应的物理表。

#### 方式二：从 Excel 导入结构

切换到 **从Excel导入** 标签页：

1. 填写 **标识**、**名称**、**物理表名**（上传文件后会自动填充，可修改）
2. **拖拽或点击选择** 一个 Excel 文件（`.xlsx` 或 `.xls`）
3. 系统自动解析 Excel 表头，推断字段名和数据类型
4. 解析完成后显示推断的表结构，可逐个编辑字段名、类型、长度等
5. 确认无误后点击 **创建**

> **提示：** 从 Excel 导入时，系统会调用 AI 自动将中文表头翻译为英文字段名，但仍建议人工检查后创建。

### 2.3 删除数据表

点击操作列的 **删除** 按钮，确认后删除。删除后关联的导入记录和报表也会受到影响，请谨慎操作。

### 2.4 跨数据库查询

如果 MySQL 中有多个数据库，可在 **数据库名** 字段指定表所在的数据库。查询时系统会自动使用 `database_name.table_name` 格式进行跨库查询。

---

## 3. 数据导入

数据导入功能将 Excel 文件中的数据批量写入已注册的数据表。

### 3.1 进入导入页面

点击导航栏中的 **数据导入**，进入导入记录列表页，展示历史导入记录的文件名、数据表、月份、行数、状态和导入时间。

点击 **新增数据导入** 按钮，进入导入表单页。

### 3.2 创建导入

在导入表单页，按以下步骤操作：

#### 步骤 1：选择数据表和填写月份

| 字段 | 说明 |
|------|------|
| **数据表** | 选择已注册的数据表（如 `运营数据`） |
| **月份** | 导入数据所属月份，格式如 `2024-01` |

#### 步骤 2：上传 Excel 文件

点击 **选择Excel文件**，选择 `.xlsx` 或 `.xls` 文件。上传后系统会：

1. 自动解析 Excel 的前 10 行数据作为预览
2. 自动匹配 Excel 列名与数据表字段
3. 生成列映射关系

#### 步骤 3：确认列映射

预览区域展示 Excel 文件的前 10 行数据，下方是列映射表格：

| 列 | 说明 |
|----|------|
| **Excel列名** | Excel 文件中的列标题 |
| **映射到字段** | 对应的数据表字段，通过下拉框选择 |
| **匹配方式** | 精确（绿色）/ 模糊（橙色）/ AI（紫色）/ 未匹配（灰色）/ 手动 |

**匹配方式说明：**

- **精确匹配** — Excel 列名与数据表字段名完全一致
- **模糊匹配** — 列名部分相似
- **AI 匹配** — 通过 AI 语义理解推荐的映射
- **未匹配** — 未能自动匹配，需手动指定
- **手动** — 用户自行选择的映射

**操作技巧：**

- 点击映射到字段下拉框，可选择 **不导入** 跳过某列
- 已映射的字段在下拉框中自动隐藏，防止重复映射
- 点击 **AI智能匹配** 按钮，让 AI 重新分析并推荐最优映射

#### 步骤 4：确认导入

确认列映射无误后，点击 **确认导入**。系统会将 Excel 中的所有行数据按映射关系写入 MySQL 数据库。导入完成后自动跳转到导入详情页，可查看导入结果。

### 3.3 查看导入记录

在导入记录列表页：

- **成功** 的记录显示绿色标签，点击 **详情** 可查看导入详情
- **失败** 的记录显示红色标签，点击 **详情** 可查看错误日志

### 3.4 Excel 文件格式要求

- 支持 `.xlsx` 和 `.xls` 格式
- 第一行应为列标题（表头）
- 数据从第二行开始
- 列的顺序和数量可以与数据表字段不完全一致，通过列映射控制

---

## 4. 报表定制

报表设计器是一个拖拽式的可视化画布，可以自由布局多种组件。

### 4.1 进入报表设计器

- **新建报表：** 点击导航栏的 **报表列表** → **新建报表**，或直接访问 `/reports/new`
- **编辑已有报表：** 在报表列表中点击某条记录的 **编辑** 按钮

### 4.2 界面布局

报表设计器分为三个区域：

```
┌────────────────────────────────────────────────────────┐
│  [返回]  报表名称              [保存] [导出▾] [分享]   │  ← 顶部工具栏
├──────────┬───────────────────────┬────────────────────┤
│ 组件库   │                       │  组件设置          │
│          │                       │                    │
│  ☷ 表格  │                       │  名称              │
│  ▤ 数据卡│      画布区域          │  数据源            │
│  ▃ 柱状图│   (网格背景)          │  SQL编辑器         │
│  ⤢ 折线图│                       │                    │
│  ◐ 饼图  │                       ├────────────────────┤
│  T 文本  │                       │  AI 分析           │
│          │                       │                    │
│          │                       │  [对话记录]        │
│          │                       │  [输入框] [发送]   │
└──────────┴───────────────────────┴────────────────────┘
  左侧面板         中间画布             右侧面板
```

### 4.3 添加组件

1. 从左侧 **组件库** 中拖拽组件到画布
2. 或使用鼠标拖入，组件自动放置到画布中心位置

系统提供 6 种组件：

| 组件 | 说明 | 默认尺寸 |
|------|------|----------|
| **文本** | 自由文本内容，如报表标题、说明 | 240×120 |
| **数据卡片** | 单值指标展示（KPI） | 200×120 |
| **表格** | 数据表格展示 | 480×260 |
| **柱状图** | ECharts 柱状图 | 400×260 |
| **折线图** | ECharts 折线图 | 400×260 |
| **饼图** | ECharts 饼图 | 320×260 |

### 4.4 拖拽与缩放

**移动组件：**

- 按住组件 **顶部标题栏** 拖动，组件完全跟随鼠标
- 拖动过程中可自由移动到任意位置
- 松手后自动吸附到最近的 20px 网格
- 如果拖动位置与其他组件重叠，拖动中显示红色边框，松手时弹回原位置并提示

**缩放组件：**

- 选中组件后，四周出现 8 个拉伸手柄（上、下、左、右四条边 + 四个角）
- 拖拽任意手柄可调整组件大小
- 松手后自动吸附网格
- 最小尺寸：150×80px

**快捷键：**

- **Delete / Backspace** — 删除选中的组件（编辑框内输入时不生效）
- **右键点击** — 选中组件

### 4.5 配置组件数据

选中画布上的组件后，右侧 **组件设置** 面板会显示该组件的配置项：

#### 通用设置

| 设置项 | 说明 |
|--------|------|
| **名称** | 组件显示名称 |
| **数据源** | 选择已注册的数据表（非文本组件） |
| **SQL** | 编写 SQL 查询语句 |

#### SQL 编辑器

- 默认显示为折叠状态，点击 **编辑** 展开
- 展开后为深色背景的代码编辑区，支持多行 SQL
- 点击 **执行** 按钮运行 SQL，查询结果将更新组件的数据预览

#### 图表选项（柱状图、折线图、饼图）

| 设置项 | 说明 |
|--------|------|
| **标题** | 图表标题，留空不显示 |
| **X轴名称** | 横轴标签名称（饼图无此项） |
| **Y轴名称** | 纵轴标签名称（饼图无此项） |

#### 文本组件

- 仅需编辑 **内容** 字段，支持多行文本

### 4.6 组件右键菜单

右键点击组件弹出操作菜单：

| 操作 | 说明 |
|------|------|
| **复制** | 复制当前组件，偏移 20px 放置 |
| **重命名** | 修改组件名称 |
| **锁定/解锁** | 锁定后不可移动和缩放 |
| **置于顶层** | 提升图层至最上层 |
| **置于底层** | 降低图层至最下层 |
| **删除** | 移除组件 |

### 4.7 使用 AI 生成 SQL

右侧面板的 **AI 分析** 区域支持自然语言查询：

1. 在输入框中输入自然语言问题，例如：`各充电站本月的充电量是多少`
2. AI 自动将问题转换为 SQL 查询语句
3. 生成的 SQL 会自动填充到当前选中组件的 SQL 编辑器中
4. 系统自动执行 SQL 并更新图表预览
5. AI 会提供 **后续建议问题**，点击标签即可快速追问

**AI 回复中包含：**

- 文字分析结果
- 生成的 SQL 代码（深色背景代码块）
- 使用的数据表名称
- 推荐的后续问题

### 4.8 保存报表

点击顶部工具栏的 **保存** 按钮：

- **新建报表** — 弹出对话框输入报表名称，保存后 URL 自动更新为报表编辑地址
- **编辑已有报表** — 直接更新，提示"报表已更新"

### 4.9 画布布局技巧

- **紧密排列：** 拖动中组件可以紧密相邻，松手时自动对齐网格
- **多组件布局：** 先放置核心组件（表格/图表），再用文本组件添加标题和说明
- **数据卡片组合：** 多个 KPI 卡片并排放置，形成指标看板
- **图层管理：** 当组件重叠时，使用右键菜单的 **置于顶层** 和 **置于底层** 调整显示顺序
- **锁定背景：** 将不需要移动的组件（如标题文本）锁定，避免误操作

### 4.10 组件数据类型说明

每个组件的 SQL 查询结果会自动按数据类型展示：

- **表格组件** — 以表格形式展示全部列和行
- **柱状图** — 第一列作为 X 轴标签，第二列作为 Y 轴数值
- **折线图** — 第一列作为 X 轴标签，后续列作为多条折线
- **饼图** — 第一列作为扇区标签，第二列作为数值比例
- **数据卡片** — 显示第一个单元格的值
- **文本组件** — 不依赖数据，显示手动输入的文本内容

---

## 5. 报表分享与导出

### 5.1 分享报表

在报表列表页或设计器中，点击 **分享** 按钮：

1. 系统生成唯一的分享链接（如 `/share/abc123def`）
2. 链接自动复制到剪贴板
3. 分享链接默认 7 天后过期
4. 接收者无需登录即可查看报表数据

### 5.2 导出报表

在报表列表页或设计器中，点击 **导出** 下拉菜单，支持三种格式：

| 格式 | 说明 |
|------|------|
| **Excel** | 导出为 `.xlsx` 文件，每个组件一个 Sheet |
| **HTML** | 导出为 HTML 网页，可在浏览器中直接查看 |
| **PDF** | 导出为 HTML 格式（通过浏览器打印为 PDF） |

### 5.3 执行报表

在报表列表页点击 **执行** 按钮，直接运行报表并查看结果（适用于经典模式报表）。画布模式的报表请在设计器中查看。

---

## 6. AI 辅助分析

### 6.1 在报表设计器中使用 AI

详见 [4.7 使用 AI 生成 SQL](#47-使用-ai-生成-sql)。

### 6.2 AI 智能匹配列映射

在数据导入的列映射环节，点击 **AI智能匹配** 按钮：

1. AI 分析 Excel 列名与数据表字段的语义关系
2. 自动填充推荐的映射关系
3. 匹配方式标记为 **AI**（紫色标签）
4. 用户可在此基础上微调

### 6.3 AI 字段翻译

从 Excel 创建数据表时，AI 自动将中文表头翻译为英文字段名，提高表结构规范性。
